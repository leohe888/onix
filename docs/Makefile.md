# Makefile

## 规则

```makefile
目标: 依赖
    命令    # 命令必须以 Tab 开头
```

## 注释

仅支持以 `#` 开头的单行注释。

## 自动变量

- `$@`：目标文件。
- `$<`：第一个依赖文件。
- `$^`：所有依赖文件。
- `$?`：比目标文件新的依赖文件

## 赋值

- `=`：使用时才展开，允许引用后面定义的变量。
- `:=`：立即展开，只使用当前已定义的变量。
- `?=`：只有当变量未定义时才赋值。
- `+=`：向变量追加值。

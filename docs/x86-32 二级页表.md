# x86-32 分页机制

在 x86-32

在 x86-32 架构中，使用二级页表来管理虚拟内存。


一个 32 位虚拟地址被划分为三部分：

- 页目录索引：10 位，用于选择页目录表中的一个条目，即页目录项（Page Directory Entry, PDE）。
- 页表索引：10 位，用于选择页表中的一个条目，即页表项（Page Table Entry, PTE）。
- 页内偏移：12 位，用于选择页内的一个字节。

PDE 的格式如下：

PTE 的格式如下：

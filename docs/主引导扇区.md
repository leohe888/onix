# 主引导扇区

基本输入输出系统（Basic Input/Output System，BIOS）在完成加电自检（Power-On Self-Test，POST）后，会将硬盘的第一个扇区，即主引导扇区（Master Boot Record，MBR），读取到内存的 0x7C00 地址处，并跳转到该地址执行引导代码。主引导扇区的总大小为 512 字节，主要包括以下三个部分：

- 引导代码：446 字节。
- 分区表：64 字节，分为 4 个条目，每个条目 16 字节。
- 结束标志：2 字节，固定为 `0x55AA`。
